# Frontend environment variables
# Copy to .env for local dev, .env.production for prod build

# For local dev with Vite proxy — leave as /api
VITE_API_URL=

# For production deployment — set to your deployed backend URL
# VITE_API_URL=https://finledger-api.onrender.com
